# Slack Bot Configuration
# Get these from https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-token-here

# App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-token-here

# Signing Secret (from Basic Information > App Credentials)
SLACK_SIGNING_SECRET=your-signing-secret-here

# Database Configuration
# Defaults to ./slack_claude.db in current working directory
# DATABASE_PATH=/path/to/slack_claude.db

# Default working directory for Claude CLI
# Defaults to the directory where the server is started if not set
# DEFAULT_WORKING_DIR=/path/to/your/projects

# Claude Code Permission Mode
# Valid modes: acceptEdits, bypassPermissions, default, delegate, dontAsk, plan
# CLAUDE_PERMISSION_MODE=bypassPermissions

# Default Model for new sessions (optional)
# Options: opus, sonnet, haiku
# DEFAULT_MODEL=opus

# Execution Timeouts
# PERMISSION_TIMEOUT=300
# USAGE_CHECK_TIMEOUT=30
# MAX_QUESTIONS_PER_CONVERSATION=10

# Multi-Agent Configuration
# PLANNER_MAX_TURNS=10
# WORKER_MAX_TURNS=30
# EVALUATOR_MAX_TURNS=10

# Permission Configuration
# AUTO_APPROVE_TOOLS=Read,Glob,Grep,LSP

# Restrict which tools Claude can use (optional)
# Use comma-separated list. Supports patterns like "Bash(git:*)"
# ALLOWED_TOOLS=Read,Glob,Grep,LSP,Edit,Write,Bash(git:*)

# File Upload Configuration
# MAX_FILE_SIZE_MB=10
# MAX_UPLOAD_STORAGE_MB=100

# GitHub repository for web viewer links (e.g., "owner/repo")
# GITHUB_REPO=

# Command Suffix Configuration
# Add a global suffix to all Slack commands (optional)
# Example: COMMAND_SUFFIX=cc results in /ls-cc, /cd-cc, /model-cc, etc.
# Valid characters: lowercase letters, numbers, underscore, hyphen
# Maximum length: calculated to keep total command ≤ 32 characters
# Note: Original commands (e.g., /ls) will NOT work when suffix is configured
# COMMAND_SUFFIX=

# Slack Message Throttling
# MESSAGE_UPDATE_THROTTLE=2.0

# Usage Cache Duration (seconds)
# USAGE_CACHE_DURATION=60

# Streaming Configuration
# MAX_ACCUMULATED_SIZE=500000
# MAX_TOOLS_DISPLAY=10
# TOOL_THREAD_THRESHOLD=500

# Display Truncation Lengths
# TRUNCATE_PATH_LENGTH=45
# TRUNCATE_CMD_LENGTH=50
# TRUNCATE_PATTERN_LENGTH=40
# TRUNCATE_URL_LENGTH=50
# TRUNCATE_TEXT_LENGTH=40
